name: Mentioning Posts

on:
  push:
    branches: [master]
    paths: 'src/mentioning-posts/**'

jobs:
  build-and-push-docker-image:
    name: Build Docker image and push to repository
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Login to Azure Container Registry
        uses: azure/docker-login@v1
        with:
          login-server: vasilvelikovkwetter.azurecr.io
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}

      - name: Build and Push Image
        run: |
          docker build ./src/mentioning-posts -t vasilvelikovkwetter.azurecr.io/mentioning-posts:latest
          docker push vasilvelikovkwetter.azurecr.io/mentioning-posts:latest

      - name: Image digest
        run: echo ${{ steps.docker_build.outputs.digest }}
