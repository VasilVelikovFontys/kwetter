import React  from 'react';
import '../../../styles/components/profile/counts/counts.css';

const Counts = () => {
    return (
        <div id='counts'>
            Counts
        </div>
    )
}

export default Counts;
